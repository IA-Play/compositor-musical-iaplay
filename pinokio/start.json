{
  "daemon": true,
  "run": [
    {
      "method": "shell.run",
      "params": {
        "message": "npm run dev -- --host 127.0.0.1 --port {{port}}",
        "on": [{ "event": "/(http:\\/\\/[0-9.:]+\\d)/", "done": true }]
      }
    },
    {
      "method": "local.set",
      "params": {
        "url": "{{input.event[1]}}"
      }
    }
  ]
}
